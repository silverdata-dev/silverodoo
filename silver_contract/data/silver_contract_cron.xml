<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron Job para Generación de Facturas Recurrentes -->
        <record id="ir_cron_generate_recurring_invoices" model="ir.cron">
            <field name="name">Silver Contract: Generar Facturas Recurrentes</field>
            <field name="model_id" ref="model_silver_contract"/>
            <field name="state">code</field>
            <field name="code">model._cron_generate_invoices()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <!--field name="numbercall">-1</field-->
            <!--field name="doall" eval="False"/-->
            <field name="active" eval="True"/>
        </record>
    </data>
</odoo>
