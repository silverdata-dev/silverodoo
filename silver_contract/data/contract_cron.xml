<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron Job para la automatización de cortes y reconexiones -->
        <record id="ir_cron_auto_suspend_reactivate_contracts" model="ir.cron">
            <field name="name">Corte y Reconexión Automática de Contratos</field>
            <field name="model_id" ref="silver_contract.model_silver_contract"/>
            <field name="state">code</field>
            <field name="code">model._cron_auto_suspend_reactivate_contracts()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <!--field name="numbercall">-1</field-->
            <!--field name="doall" eval="False"/-->
        </record>
    </data>
</odoo>