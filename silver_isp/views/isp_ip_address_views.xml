<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- isp.ip.address -->
        <record id="action_isp_ip_address" model="ir.actions.act_window">
            <field name="name">Direcciones IP ISP</field>
            <field name="res_model">isp.ip.address</field>
            <field name="view_mode">tree,form</field>
        </record>

        <record id="view_isp_ip_address_tree" model="ir.ui.view">
            <field name="name">isp.ip.address.tree</field>
            <field name="model">isp.ip.address</field>
            <field name="arch" type="xml">
                <tree string="Direcciones IP ISP">
                    <field name="name"/>
                    <field name="core_id"/>
                    <field name="olt_id"/>
                    <field name="card_id"/>
                    <field name="port_id"/>
                    <field name="radius_id"/>
                    <field name="is_tr_069"/>
                </tree>
            </field>
        </record>

        <record id="view_isp_ip_address_form" model="ir.ui.view">
            <field name="name">isp.ip.address.form</field>
            <field name="model">isp.ip.address</field>
            <field name="arch" type="xml">
                <form string="Dirección IP ISP">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="core_id"/>
                            <field name="olt_id"/>
                            <field name="card_id"/>
                            <field name="port_id"/>
                            <field name="radius_id"/>
                            <field name="is_tr_069"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>
